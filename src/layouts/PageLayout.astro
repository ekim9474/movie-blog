---
import GenLayout from "./GenLayout.astro";

const { title } = Astro.props;
---

<!-- realized that this most pages will follow this format, so this will avoid breaking things already using GenLayout -->
<GenLayout title={title}>
  <div class="genericContainer">
    <slot />
  </div>
</GenLayout>

<style>
  .genericContainer {
    width: 60%;
    height: 100%;
  }
</style>
